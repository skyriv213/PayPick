import {globalStyle, style} from "@vanilla-extract/css";

export const mapSize = style({
  width: '100dvw',
  height: '100dvh'
});

export const map = style({
  width: '100dvh',
  height: '100dvh'
});